-module(draft_repository).
-author("weemen").

%% API
-export([get_by_id/1, save/1, add_to_cache/1, remove_from_cache]).

%%get_by_id(Id) ->
%%save(Pid) ->
%%add_to_cache(Id) ->
%%remove_from_cache(Id) ->
%%load_from_event_story(Id) ->
